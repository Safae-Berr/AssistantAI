# --- Core web stack ---
fastapi
uvicorn[standard]
python-multipart

# --- Database ---
sqlalchemy
psycopg2-binary           # for postgres (prod)
alembic                   # migrations (you'll use this later)

# --- Config & env ---
python-dotenv
pydantic
pydantic-settings
email-validator           # required for pydantic EmailStr

# --- Security: auth ---
python-jose[cryptography] # JWT signing
passlib[bcrypt]           # password hashing
bcrypt<4.1                # pin: passlib has issues with 4.1+
pyotp                     # TOTP (Google Authenticator)
segno                     # QR code generation for MFA setup

# --- AI/ML (kept for later modules) ---
torch
numpy
pandas
matplotlib
scikit-learn

# --- Tests ---
pytest
httpx                     # for FastAPI TestClient